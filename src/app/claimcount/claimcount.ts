export interface Claim {
    fin_total_claims: number;
    fin_total_claims_sum: number;
    integ_total_normal_claims: number;
    integ_total_normal_claims_sum: number;
    integ_total_off_smart_claims: number;
    integ_total_off_smart_claims_sum: number;
    stg_uap_total_normal_claims: number;
    stg_uap_total_normal_claims_sum: number;

  }